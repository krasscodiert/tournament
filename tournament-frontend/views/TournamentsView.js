import m from "mithril";

import { ContentView } from "./ContentView";

export let TournamentsView = {
    view(vnode) {
        return (
            <ContentView>
                Hallo. Hier stehen die Tournaments
            </ContentView>
        )
    }
};
