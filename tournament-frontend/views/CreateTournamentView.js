import m from "mithril";

import { ContentView } from "./ContentView";

export let CreateTournamentView = {
    view(vnode) {
        return (
            <ContentView>
                Erstelle ein Tournament
            </ContentView>
        )
    }
};
