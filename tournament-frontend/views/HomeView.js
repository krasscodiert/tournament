import m from "mithril";

import { ContentView } from "./ContentView";

export let HomeView = {
    view(vnode) {
        return (
            <ContentView>
                Das ist die home seite
            </ContentView>
        )
    }
};
